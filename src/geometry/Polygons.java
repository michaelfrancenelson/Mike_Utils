package geometry;

public class Polygons {

	public static double parallelogramArea(
			double sideLength1, double sideLength2, double width)
	{
		return(0.5 * (sideLength1 + sideLength2) * width);
	}
	
}
